import PlaceholderPage from "../components/PlaceholderPage";

export default function Dashboard() {
  return (
    <PlaceholderPage
      title="Your dashboard"
      description="Your personalized study hub — recent subjects, quick actions and streaks will live here. Coming in a later build step."
    />
  );
}
