import PlaceholderPage from "../components/PlaceholderPage";

export default function Settings() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Preferences like theme and notifications will live here. Coming in a later build step."
    />
  );
}
